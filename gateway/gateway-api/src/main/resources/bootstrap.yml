spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.66:8848 #本次单机
        enabled: true
      config:
        file-extension: yaml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
